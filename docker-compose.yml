
version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    ports:
      - "8081:80"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_URL=http://devops-frontend-alb-261429494.us-east-1.elb.amazonaws.com/api
